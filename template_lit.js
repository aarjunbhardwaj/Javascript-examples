// template literals

for(let num = 1; num<=10 ; num++){
    let tableOf = 13;
//console.log(tableOf+"*"+num+"="+tableOf*num);    
// insted of this we can use templete litrels
console.log(`${tableOf}*${num}=${tableOf*num}`);

}
