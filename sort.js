// const months = ['march','dec','jan','april','feb'];
// console.log(months.sort());

// its converting first letter of word in ascending order

const array1 = [1,4,32,24,10000,500,];
console.log(array1.sort());

// here is also its doing according to first letter
// sort () method produce incorrect result when sorting numbers.